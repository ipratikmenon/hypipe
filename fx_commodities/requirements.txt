# Broker bridge — Windows only; everything else runs on any OS
MetaTrader5>=5.0.45; platform_system == "Windows"

pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
python-dotenv>=1.0.0
requests>=2.31.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Model layer (Phase 3)
lightgbm>=4.0.0
scikit-learn>=1.4.0

# Dev
pytest>=8.0.0
